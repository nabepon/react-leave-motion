import ReactTransitionGroup from 'react-addons-transition-group';
const LeaveTransitionGroup = ReactTransitionGroup;
export default LeaveTransitionGroup;
// 将来的にはReactTransitionGroupを拡張したコンポーネントをここで作る
